# GitHub Pages — nammedi

Published at: **https://zhandreya.github.io/nammedi/**

1. Repo: `zhandreya/nammedi`, branch `main`
2. Settings → Pages → Source: **GitHub Actions**
3. Workflow: `.github/workflows/deploy.yml`

`supabase-config.js` already contains the live project URL and anon key, so deploy works even without Actions secrets.
